#include"common.h"

/**
 * WARN: Needs to manually release the returned memory
 */
char* say_hello(int client_socket, char *neu_id);

/**
 * WARN: Needs to manually release the returned memory
 */
char* solve(int client_socket, char *status);

